\textbf{Exercise 1:} Give formation sequences of the following
\begin{enumerate}
    \item $(\neg p_2 \to (p_3 \lor (p_1 \leftrightarrow p_2))) \land \neg p_3$
    \begin{mdframed}
        Let \vspace{-0.7cm}
        \begin{align*}
            \{\phi_i\} = \{&p_1, p_2, p_3, \\ &\neg p_2, \neg p_3, (p_1 \leftrightarrow p_2), \\ &(p_3 \lor (p_1 \leftrightarrow p_2)), (\neg p_2 \rightarrow (p_3 \lor (p_1 \leftrightarrow p_2)))),  (\neg p_2 \rightarrow (p_3 \lor (p_1 \leftrightarrow p_2))))\land \neg p_3\}
        \end{align*}
    \end{mdframed}
\end{enumerate}

% \[\int \frac{x}{(1 + x^2)^2}dx\]
% Let $u = 1 + x^2$. Then $du = 2xdx$. Then 
% \begin{align*}
%     \int \frac{x}{(1 + x^2)^2}dx &= \int\frac{\frac{1}{2}(2xdx)}{(1 + x^2)^2} &&\text{(rearrange)}\\
%     &=\int\frac{\frac{1}{2}(du)}{(u)^2} &&\text{(substitute $u = 1 + x^2$ and $du=2xdx$)}\\
%     &=\frac{1}{2}\int\frac{1}{u^2}du &&\text{(clean up the expression)}\\
%     &= -\frac{1}{2u} + C&&\text{(integrate)} \\
%     &= -\frac{1}{2(1 + x^2)} + C&&\text{(substitute back in $u = 1 + x^2$)}
% \end{align*}

% \begin{align*}
%     \frac{x^2 - 2}{x^2 - 1} - \frac{1}{x-1} = \frac{3}{x^2 - 1} &\iff  \frac{x^2 - 2}{x^2 - 1} - \frac{3}{x^2 - 1} = \frac{1}{x-1} &&\text{(subtract terms to the other side)}\\
%     &\iff \frac{x^2 - 2 - 3}{x^2 - 1} = \frac{1}{x-1} &&\text{(combine terms with same denom)} \\
%     &\iff \frac{x^2 - 5}{x^2 - 1} = \frac{1}{x-1} &&\text{(simplify)} \\
%     &\iff (x^2 - 5)(x-1) = x^2 - 1 &&\text{(cross-multiply)} \\
%     &\iff (x^2 - 5)(x-1) = (x-1)(x+1) &&\text{(factor the $x^2 - 1$ term)} \\
%     &\text{nótese que x = 1 es una solución de la ecuación anterior}\\
%     &\iff (x^2 - 5)\cancel{(x-1)} = \cancel{(x-1)}(x+1) &&\text{(cancel terms)} \\
%     &\iff (x^2 - 5) =(x+1) \\
%     &\iff x^2 -x - 6 =0 &&\text{(simplify)}\\
%     &\iff (x-3)(x+2)=0 &&\text{(factor)}
% \end{align*}
% Vemos que $(x-3)(x+2)=0 \iff x = 3, x = -2$. \\ En conclusión $x = 3, x = -2$ y $x = 1$ son las soluciones. 